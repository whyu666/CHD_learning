#include <iostream>
#include <cmath>
#include <iomanip>
#include <vector>
#include <algorithm>
using namespace std;
int main() 
{
	double a,b,c;
	cin>>a>>b>>c; 
	double p,s;
	p=(a+b+c)/2;
	s=sqrt(p*(p-a)*(p-b)*(p-c));
	cout<<setiosflags(ios::fixed)<<setprecision(1)<<s;
	return 0;
}
